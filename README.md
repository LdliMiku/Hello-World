# Hello-World

这是我 fork 的 GitHub 官方入门项目（octocat/Hello-World），用来学习 Git 和 GitHub。

## 关于我

- 昵称：LdliMiku
- 这是我第一次真正上手 Git，已经学会了 fork、克隆、修改、提交、推送。
- 我正在弄清这些工具是干嘛用的，让机器替我做重复的事，我专注理解概念。

## 我学到的

- 用 `git add` 挑选要记录的改动
- 用 `git commit` 打一个存档点
- 用 `git push` 把改动推回 GitHub

## 目标

把 GitHub 的核心流程玩明白，之后用 Fork 和 Pull Request 参与开源。
